---
interface Props {
	body: string;
}

const { body } = Astro.props;

const words = body.trim().split(/\s+/).length;
const minutes = Math.max(1, Math.round(words / 200));
---

<span class="text-md">{minutes} min read</span>
